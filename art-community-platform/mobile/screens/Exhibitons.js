import { StyleSheet, Text, View } from "react-native";

const Exhibitions = () => {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Exhibitions Screen</Text>
    </View>
  );
};

export default Exhibitions;
